<%= form_for :topic do |f| %>

	 <div class="field">
	 	<%= f.label :title %><br>
	 	<%= f.rext_field :title %>
	 </div>
	 <div class="field">
	 	<%= f.label :description %><br>
	 	<%= f.text_area :description %>
	 </div>
	 <div class=